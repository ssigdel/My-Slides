// const imageUpload = (inputType) => {
//     if (inputType === 'file'){
//         bodyInput.addEventListener('change', () => {
//             const inputFile = new FileReader();

//             inputFile.addEventListener('load', () => {
//                 console.log(inputFile.result)
//             })

//             inputFile.readAsDataURL(this.files[0])
//         })
//     }
// }